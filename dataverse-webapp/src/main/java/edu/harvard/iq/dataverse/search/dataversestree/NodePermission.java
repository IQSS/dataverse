package edu.harvard.iq.dataverse.search.dataversestree;

public enum NodePermission {
    /** User can see this node, but cannot select it */
    VIEW,
    /** User can see and select this node */
    SELECT
}
