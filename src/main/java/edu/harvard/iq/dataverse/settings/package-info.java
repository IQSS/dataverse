/**
 * Settings are a persistent {@code Map<String,String>}, used to allow sysadmins
 * to setup Datavarse by means other than environment variables.
 * There is also possibility to define setting values from property files (non-persistent).
 * Persistent settings are have higher priority (they override non-persistent ones)
 */
package edu.harvard.iq.dataverse.settings;
